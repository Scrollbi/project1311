<Window x:Class="project2.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация в системе" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход в систему" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,30"/>

        <TextBlock Grid.Row="1" Text="Логин:" 
                   FontSize="14" FontWeight="SemiBold"
                   Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="txtLogin" 
                 Height="35" 
                 FontSize="14"
                 Padding="10,0"
                 Margin="0,0,0,15"
                 BorderThickness="1"/>

        <TextBlock Grid.Row="3" Text="Пароль:" 
                   FontSize="14" FontWeight="SemiBold"
                   Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="4" x:Name="txtPassword" 
                     Height="35"
                     FontSize="14"
                     Padding="10,0"
                     Margin="0,0,0,20"
                     BorderThickness="1"/>

        
        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10">
            <Button x:Name="btnLogin" Content="Войти" 
                    Width="120" Height="35" 
                    Margin="0,0,15,0" 
                    Background="#3498db"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="btnLogin_Click">
                
            </Button>
        </StackPanel>

        <TextBlock Grid.Row="6" x:Name="txtStatus" 
                   Text="" HorizontalAlignment="Center"
                   FontSize="12" Foreground="#7f8c8d"/>
    </Grid>
</Window>